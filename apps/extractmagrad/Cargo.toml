[package]
name = "extractmagrad"
version.workspace = true
edition.workspace = true

[[bin]]
name = "extractmagrad"
path = "src/main.rs"

[dependencies]
imod-mrc.workspace = true
clap = { version = "4", features = ["derive"] }
