// Mirrors `IMOD/Etomo/src/etomo/ui/swing/PeetProcessPanel.java`, an `AxisProcessPanel`
// subclass.  It instantiates the base component from
// `AxisProcessPanel.slint` with the flags its constructor passes to
// `super`, and adds nothing to `panelProcessSelect`:
//
// `super(AxisID.ONLY, manager, false, true, InterfaceType.PEET, false,
//  axisProgressPanel)` then `createProcessControlPanel()` and
//  `initializePanels()` (`:31-36`).
//
// Overrides nothing.  It never calls `setBackground`, so the panel keeps the
// Metal default grey; `popupChunkWarnings` is false here, which is behaviour
// rather than appearance.
//
// Appearance only: no callbacks, no state, static defaults everywhere.

import { EtomoStyle } from "../../etomo-ui-common/ui/widgets.slint";
import { Colors } from "MainWindowWidgets.slint";
import { AxisProcessPanel } from "AxisProcessPanel.slint";

export component PeetProcessPanel inherits Rectangle {
    in property <color> panel-background: Colors.default-panel;
    background: root.panel-background;
    AxisProcessPanel {
        width: 100%;
        height: 100%;
        axis-id: "ONLY";
        panel-background: root.panel-background;
        alt-parallel-loc: false;
    }
}

export component PeetProcessPanelWindow inherits Window {
    title: "eTomo PeetProcessPanel";
    preferred-width: 700px;
    preferred-height: 500px;
    background: EtomoStyle.background;
    PeetProcessPanel { }
}
