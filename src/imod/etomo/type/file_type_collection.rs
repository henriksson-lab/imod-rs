//! `IMOD/Etomo/src/etomo/type/FileTypeCollection.java`.
//!
//! The whole source unit is a package-private marker interface with no members:
//!
//! ```text
//! package etomo.type;
//!
//! interface FileTypeCollection {
//!
//! }
//! ```
//!
//! It is mirrored as an empty trait so that a translated `FileType.INSTANCES` map can
//! name it.  Nothing in the vendored source implements it or reads a value typed by it.
#![allow(dead_code)]

/// Java package-private interface `FileTypeCollection`.  Declares no members.
pub trait FileTypeCollection {}
