//! `IMOD/Etomo/src/etomo/type/ImodVersion.java`.
//!
//! A class containing the current Imod version.  It declares no methods, only the two
//! `public static final String` constants, so the whole unit is these two items.
#![allow(dead_code)]

/// Java `rcsid`.
pub const RCSID: &str = "$Id$";

/// Java `CURRENT_VERSION`.
pub const CURRENT_VERSION: &str = "5.2.17";
