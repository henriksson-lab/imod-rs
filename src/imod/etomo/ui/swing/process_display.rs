//! `IMOD/Etomo/src/etomo/ui/swing/ProcessDisplay.java`.

#![allow(dead_code)]

/// Java marker interface `ProcessDisplay`.
///
/// It intentionally has no Rust members: as in Java, its only role is to
/// identify panels whose parameters can be consumed by process-series code.
pub trait ProcessDisplay {}
